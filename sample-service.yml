# HTTP Server
server:  
  port: 0   # HTTP port

# Discovery Server Access
eureka:  
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
